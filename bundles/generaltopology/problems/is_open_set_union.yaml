slug: is_open_set_union
kind: theorem
title: A Union of Open Sets, However Many
statement: |-
  Here the number of sets does not matter at all. A point of the union lies in one of them, that one is open, so it supplies a ball around the point — and the ball, being inside that one set, is inside the union.

  A collection of sets is itself a set: `F : Set (Set X)`. Its union is `⋃₀ F`, the points belonging to at least one member, and `x ∈ ⋃₀ F` unfolds to `∃ U, U ∈ F ∧ x ∈ U`.
concepts:
- open_sets
stub: |-
  theorem MetricSpace.isOpenSet_sUnion {X : Type u} (M : MetricSpace X) {F : Set (Set X)}
      (h : ∀ U ∈ F, M.IsOpenSet U) : M.IsOpenSet (⋃₀ F) := sorry
reference: |-
  theorem MetricSpace.isOpenSet_sUnion {X : Type u} (M : MetricSpace X) {F : Set (Set X)}
      (h : ∀ U ∈ F, M.IsOpenSet U) : M.IsOpenSet (⋃₀ F) := by
    intro x hx
    obtain ⟨U, hUF, hxU⟩ := hx
    obtain ⟨ε, hε, hsub⟩ := h U hUF x hxU
    exact ⟨ε, hε, fun y hy => ⟨U, hUF, hsub hy⟩⟩
spec: |-
  example : ∀ {X : Type u} (M : MetricSpace X) {F : Set (Set X)}
      (h : ∀ U ∈ F, M.IsOpenSet U), M.IsOpenSet (⋃₀ F) := _root_.GeneralTopology.MetricSpace.isOpenSet_sUnion
preamble: |
  universe u
  namespace GeneralTopology
  structure MetricSpace (X : Type u) where
    dist : X → X → ℝ
    dist_comm : ∀ {x y}, dist x y = dist y x
    dist_triangle : ∀ x y z, dist x z ≤ dist x y + dist y z
    dist_eq_zero : ∀ {x y}, dist x y = 0 ↔ x = y

  def MetricSpace.ball {X : Type u} (M : MetricSpace X) (x : X) (ε : ℝ) : Set X :=
    {y | M.dist x y < ε}

  def MetricSpace.IsOpenSet {X : Type u} (M : MetricSpace X) (U : Set X) : Prop :=
    ∀ x ∈ U, ∃ ε > 0, M.ball x ε ⊆ U

  end GeneralTopology
  open GeneralTopology
description: Show that `⋃₀ F` is open when every member of `F` is. Take the membership apart for the set the point came from, get a ball from that set's openness, and offer the same ball; a point of it lies in that member, hence in the union.
stub_names:
- MetricSpace.isOpenSet_sUnion
namespace: GeneralTopology
seq: 35
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/TopologicalSpaces.lean
  line: 104
  closure_rounds: 1
  check_kind: theorem
