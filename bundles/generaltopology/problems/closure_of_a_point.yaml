slug: closure_of_a_point
kind: example
title: The Closure of a Single Point
statement: |-
  What does closing one point add? A point `x` lies in the closure of `{y}` when every neighbourhood of `x` meets `{y}`, which is to say contains `y`. Only the open sets need be tested, since every neighbourhood has one inside it.

  So `x ∈ T.closure {y}` says exactly: every open set containing `x` contains `y`.

  Read as a comparison, that same condition says the closure of `{x}` lies inside the closure of `{y}`. The closure of `{y}` is a closed set, so it contains the closure of `{x}` exactly when it contains `x`.
concepts:
- separation_closures
stub: |-
  theorem TopologicalSpace.mem_closure_singleton {X : Type u} (T : TopologicalSpace X) {x y : X} :
      x ∈ T.closure {y} ↔ ∀ U, T.IsOpen U → x ∈ U → y ∈ U := sorry

  theorem TopologicalSpace.closure_singleton_iff {X : Type u} (T : TopologicalSpace X) {x y : X} :
      T.closure {x} ⊆ T.closure {y} ↔ ∀ U, T.IsOpen U → x ∈ U → y ∈ U := sorry
reference: |-
  theorem TopologicalSpace.mem_closure_singleton {X : Type u} (T : TopologicalSpace X) {x y : X} :
      x ∈ T.closure {y} ↔ ∀ U, T.IsOpen U → x ∈ U → y ∈ U := by
    constructor
    · intro hx U hU hxU
      obtain ⟨z, hzU, hzy⟩ :=
        (TopologicalSpace.mem_closure_iff T).mp hx U (TopologicalSpace.nbhd_of_isOpen T hU hxU)
      rw [show z = y from hzy] at hzU
      exact hzU
    · intro h
      apply (TopologicalSpace.mem_closure_iff T).mpr
      rintro N ⟨U, hU, hxU, hUN⟩
      exact ⟨y, hUN (h U hU hxU), rfl⟩

  theorem TopologicalSpace.closure_singleton_iff {X : Type u} (T : TopologicalSpace X) {x y : X} :
      T.closure {x} ⊆ T.closure {y} ↔ ∀ U, T.IsOpen U → x ∈ U → y ∈ U := by
    constructor
    · intro h
      exact (TopologicalSpace.mem_closure_singleton T).mp
        (h (TopologicalSpace.subset_closure T {x} rfl))
    · intro h
      refine TopologicalSpace.closure_min T (TopologicalSpace.isClosed_closure T {y}) ?_
      intro z hz
      rw [show z = x from hz]
      exact (TopologicalSpace.mem_closure_singleton T).mpr h
spec: |-
  example : ∀ {X : Type u} (T : TopologicalSpace X) {x y : X}, x ∈ T.closure {y} ↔ ∀ U, T.IsOpen U → x ∈ U → y ∈ U := _root_.GeneralTopology.TopologicalSpace.mem_closure_singleton
  example : ∀ {X : Type u} (T : TopologicalSpace X) {x y : X}, T.closure {x} ⊆ T.closure {y} ↔ ∀ U, T.IsOpen U → x ∈ U → y ∈ U := _root_.GeneralTopology.TopologicalSpace.closure_singleton_iff
preamble: |
  universe u v
  namespace GeneralTopology
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  def TopologicalSpace.IsClosed {X : Type u} (T : TopologicalSpace X) (C : Set X) : Prop
      := T.IsOpen Cᶜ

  def TopologicalSpace.IsNbhd {X : Type u} (T : TopologicalSpace X) (x : X) (N : Set X) : Prop :=
    ∃ U, T.IsOpen U ∧ x ∈ U ∧ U ⊆ N

  theorem TopologicalSpace.nbhd_of_isOpen {X : Type u} (T : TopologicalSpace X) {U : Set X}
      (hU : T.IsOpen U) {x : X} (hx : x ∈ U) : T.IsNbhd x U :=
    ⟨U, hU, hx, subset_rfl⟩

  theorem TopologicalSpace.isClosed_sInter {X : Type u} (T : TopologicalSpace X) {F : Set (Set X)}
      (h : ∀ C ∈ F, T.IsClosed C) : T.IsClosed (⋂₀ F) := by
    show T.IsOpen _
    rw [Set.compl_sInter]
    apply T.isOpen_sUnion
    rintro U ⟨C, hCF, rfl⟩
    exact h C hCF

  def TopologicalSpace.closure {X : Type u} (T : TopologicalSpace X) (S : Set X) : Set X :=
    ⋂₀ {C | T.IsClosed C ∧ S ⊆ C}

  theorem TopologicalSpace.subset_closure {X : Type u} (T : TopologicalSpace X) (S : Set X)
      : S ⊆ T.closure S := by
    intro x hx C hC
    exact hC.2 hx

  theorem TopologicalSpace.isClosed_closure {X : Type u} (T : TopologicalSpace X) (S : Set X) :
      T.IsClosed (T.closure S) :=
    T.isClosed_sInter (fun _ hC => hC.1)

  theorem TopologicalSpace.closure_min {X : Type u} (T : TopologicalSpace X) {S C : Set X}
      (hC : T.IsClosed C) (hSC : S ⊆ C) : T.closure S ⊆ C := by
    intro x hx
    exact hx C ⟨hC, hSC⟩

  theorem TopologicalSpace.mem_closure_iff {X : Type u} (T : TopologicalSpace X) {S : Set X} {x : X} :
      x ∈ T.closure S ↔ ∀ N, T.IsNbhd x N → (N ∩ S).Nonempty := by
    constructor
    · rintro hx N ⟨U, hU, hxU, hUN⟩
      by_contra h
      have hSU : S ⊆ Uᶜ := by
        intro y hy hyU
        exact h ⟨y, hUN hyU, hy⟩
      have hcl : T.IsClosed Uᶜ := by
        show T.IsOpen _
        rwa [compl_compl]
      exact hx Uᶜ ⟨hcl, hSU⟩ hxU
    · intro h C hC
      by_contra hxC
      obtain ⟨y, hyC, hyS⟩ := h Cᶜ ⟨Cᶜ, hC.1, hxC, subset_rfl⟩
      exact hyC (hC.2 hyS)

  end GeneralTopology
  open GeneralTopology
description: Prove `TopologicalSpace.mem_closure_singleton` and `TopologicalSpace.closure_singleton_iff`. Both rest on `TopologicalSpace.mem_closure_iff` and `TopologicalSpace.nbhd_of_isOpen`; a membership in a singleton is an equation, which `show` restates for `rw`. The second reads the first at `x` and closes its other direction with `TopologicalSpace.closure_min`.
stub_names:
- TopologicalSpace.mem_closure_singleton
- TopologicalSpace.closure_singleton_iff
namespace: GeneralTopology
seq: 149
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/Separation.lean
  line: 235
  closure_rounds: 1
  check_kind: theorem
