slug: closed_in_compact
kind: theorem
title: A Closed Subset of a Compact Set
statement: |-
  A closed subset of a compact space is compact, and the proof of that never used that the ambient set was the whole space. Throw the complement of the closed set in with the cover, keep the finitely many that come back, and throw the complement away again.

  Stated of a compact set rather than a compact space, the same argument gives what this concept needs: a set is compact as soon as it is closed and lies inside something compact.
concepts:
- heine_borel
stub: |-
  theorem TopologicalSpace.isCompact_closed_subset {X : Type u} (T : TopologicalSpace X) {K : Set X}
      (hK : T.IsCompact K) {C : Set X} (hC : T.IsClosed C) (hCK : C ⊆ K) :
      T.IsCompact C := sorry
reference: |-
  theorem TopologicalSpace.isCompact_closed_subset {X : Type u} (T : TopologicalSpace X) {K : Set X}
      (hK : T.IsCompact K) {C : Set X} (hC : T.IsClosed C) (hCK : C ⊆ K) :
      T.IsCompact C := by
    intro F hFo hcov
    have hopen : ∀ U ∈ insert Cᶜ F, T.IsOpen U := by
      rintro U (rfl | hU)
      exacts [hC, hFo U hU]
    have hKcov : K ⊆ ⋃₀ insert Cᶜ F := by
      intro x _
      by_cases hxC : x ∈ C
      · obtain ⟨U, hUF, hxU⟩ := hcov hxC
        exact ⟨U, Set.mem_insert_of_mem _ hUF, hxU⟩
      · exact ⟨Cᶜ, Set.mem_insert _ _, hxC⟩
    obtain ⟨G, hGF, hGfin, hGcov⟩ := hK _ hopen hKcov
    refine ⟨G ∩ F, Set.inter_subset_right, hGfin.subset Set.inter_subset_left, fun x hx => ?_⟩
    obtain ⟨U, hUG, hxU⟩ := hGcov (hCK hx)
    exact ⟨U, ⟨hUG, (hGF hUG).resolve_left (fun e => (e ▸ hxU) hx)⟩, hxU⟩
spec: |-
  example : ∀ {X : Type u} (T : TopologicalSpace X) {K : Set X}
      (hK : T.IsCompact K) {C : Set X} (hC : T.IsClosed C) (hCK : C ⊆ K), T.IsCompact C := _root_.GeneralTopology.TopologicalSpace.isCompact_closed_subset
preamble: |
  universe u v
  namespace GeneralTopology
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  def TopologicalSpace.IsClosed {X : Type u} (T : TopologicalSpace X) (C : Set X) : Prop
      := T.IsOpen Cᶜ

  def TopologicalSpace.IsCompact {X : Type u} (T : TopologicalSpace X) (S : Set X) : Prop :=
    ∀ F : Set (Set X), (∀ U ∈ F, T.IsOpen U) → S ⊆ ⋃₀ F →
      ∃ G ⊆ F, G.Finite ∧ S ⊆ ⋃₀ G

  end GeneralTopology
  open GeneralTopology
description: Prove `TopologicalSpace.isCompact_closed_subset`, which is the earlier `TopologicalSpace.isCompact_of_isClosed` with `Set.univ` replaced by a compact `K`. `insert Cᶜ F` is the enlarged cover, `G ∩ F` the collection with `Cᶜ` thrown away, and `Or.resolve_left` rules out the discarded member at a point of `C`.
stub_names:
- TopologicalSpace.isCompact_closed_subset
namespace: GeneralTopology
seq: 231
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/CompactHausdorff.lean
  line: 457
  closure_rounds: 1
  check_kind: theorem
