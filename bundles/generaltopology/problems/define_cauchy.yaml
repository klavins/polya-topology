slug: define_cauchy
kind: definition
title: Cauchy Sequences
statement: |-
  Convergence names the limit. A sequence can be seen to be settling down without naming it, by the terms growing close to one another rather than to anything outside.

  A sequence is *Cauchy* when for every tolerance `ε > 0` there is an index past which any two terms are within `ε` of each other. Two indices are quantified here, not one, and both are asked to be past the same `k`.

  The condition mentions no point of the space but the terms themselves, and that is what makes it useful: it can be checked when the limit is not yet in hand, and even when there is none.
concepts:
- cauchy_complete
stub: 'def MetricSpace.CauchySeq {X : Type u} (M : MetricSpace X) (x : ℕ → X) : Prop := sorry'
reference: |-
  def MetricSpace.CauchySeq {X : Type u} (M : MetricSpace X) (x : ℕ → X) : Prop :=
    ∀ ε > 0, ∃ k : ℕ, ∀ m ≥ k, ∀ n ≥ k, M.dist (x m) (x n) < ε
spec: |-
  example {X : Type u} (M : MetricSpace X) (x : ℕ → X) : _root_.GeneralTopology.MetricSpace.CauchySeq M x ↔ (∀ ε > 0, ∃ k : ℕ, ∀ m ≥ k, ∀ n ≥ k, M.dist (x m) (x n) < ε) := by unfold _root_.GeneralTopology.MetricSpace.CauchySeq; first | exact Iff.rfl | grind
  example (X : Type) (M : MetricSpace X) (x : ℕ → X) :
      M.CauchySeq x ↔ ∀ ε > 0, ∃ k : ℕ, ∀ m ≥ k, ∀ n ≥ k, M.dist (x m) (x n) < ε := Iff.rfl
preamble: |
  universe u v
  namespace GeneralTopology
  structure MetricSpace (X : Type u) where
    dist : X → X → ℝ
    dist_comm : ∀ {x y}, dist x y = dist y x
    dist_triangle : ∀ x y z, dist x z ≤ dist x y + dist y z
    dist_eq_zero : ∀ {x y}, dist x y = 0 ↔ x = y

  end GeneralTopology
  open GeneralTopology
description: 'Define `MetricSpace.CauchySeq`, taking a metric `M` on `X` and a sequence `x : ℕ → X`: for every `ε > 0` there is a `k : ℕ` with `∀ m ≥ k, ∀ n ≥ k, M.dist (x m) (x n) < ε`, the two indices bound one after the other rather than together.'
stub_names:
- MetricSpace.CauchySeq
namespace: GeneralTopology
seq: 192
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/Sequences.lean
  line: 405
  closure_rounds: 1
  check_kind: definition
