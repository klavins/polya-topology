slug: is_open_set_inter
kind: theorem
title: The Intersection of Two Open Sets
statement: |-
  A point of `U ∩ V` has room inside `U` and room inside `V` — two radii, one from each. The smaller of the two serves for both, and it is still positive.

  This is where *two* matters. For infinitely many open sets the radii could shrink towards zero with no positive number below them all, and the intersection need not be open.
concepts:
- open_sets
stub: |-
  theorem Metric.isOpenSet_inter {X : Type u} (M : Metric X) {U V : Set X}
      (hU : M.IsOpenSet U) (hV : M.IsOpenSet V) : M.IsOpenSet (U ∩ V) := sorry
reference: |-
  theorem Metric.isOpenSet_inter {X : Type u} (M : Metric X) {U V : Set X}
      (hU : M.IsOpenSet U) (hV : M.IsOpenSet V) : M.IsOpenSet (U ∩ V) := by
    intro x hx
    obtain ⟨a, ha, hau⟩ := hU x hx.1
    obtain ⟨b, hb, hbv⟩ := hV x hx.2
    refine ⟨min a b, lt_min ha hb, ?_⟩
    intro y hy
    rw [M.mem_ball] at hy
    exact ⟨hau (lt_of_lt_of_le hy (min_le_left a b)), hbv (lt_of_lt_of_le hy (min_le_right a b))⟩
spec: |-
  example : ∀ {X : Type u} (M : Metric X) {U V : Set X}
      (hU : M.IsOpenSet U) (hV : M.IsOpenSet V), M.IsOpenSet (U ∩ V) := _root_.GeneralTopology.Metric.isOpenSet_inter
preamble: |
  universe u
  namespace GeneralTopology
  structure Metric (X : Type u) where
    dist : X → X → ℝ
    symm : ∀ {x y}, dist x y = dist y x
    triangle : ∀ x y z, dist x z ≤ dist x y + dist y z
    eq_zero : ∀ {x y}, dist x y = 0 ↔ x = y

  def Metric.ball {X : Type u} (M : Metric X) (x : X) (ε : ℝ) : Set X :=
    {y | M.dist x y < ε}

  theorem Metric.mem_ball {X : Type u} (M : Metric X) {x y : X} {ε : ℝ} :
      y ∈ M.ball x ε ↔ M.dist x y < ε := Iff.rfl

  def Metric.IsOpenSet {X : Type u} (M : Metric X) (U : Set X) : Prop :=
    ∀ x ∈ U, ∃ ε > 0, M.ball x ε ⊆ U

  end GeneralTopology
  open GeneralTopology
description: Show that `U ∩ V` is open when `U` and `V` are. Take a point apart with `hx.1` and `hx.2`, take each hypothesis apart with `obtain`, and offer `min a b`, which `lt_min` shows positive; `min_le_left` and `min_le_right` then carry a point of the smaller ball into each of the two.
stub_names:
- Metric.isOpenSet_inter
namespace: GeneralTopology
seq: 34
provenance:
  license: CC-BY-SA-4.0
  source: extract@5060c35
  file: PolyaTopology/GeneralTopology/TopologicalSpaces.lean
  line: 81
  closure_rounds: 1
  check_kind: theorem
