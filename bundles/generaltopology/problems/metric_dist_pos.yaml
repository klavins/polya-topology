slug: metric_dist_pos
kind: theorem
title: Distinct Points Lie a Positive Distance Apart
statement: |-
  The axioms give `M.dist x y = 0` exactly when `x = y`, and the distance is never negative. Between distinct points it is therefore strictly positive.

  That is the fact every separation argument in a metric space begins with, since the radius it works with is read off from it. A real number that is at least `0` is either greater than `0` or equal to it, which is what `lt_or_eq_of_le` says of `MetricSpace.nonneg M x y`.
concepts:
- separation_examples
stub: 'theorem MetricSpace.dist_pos {X : Type u} (M : MetricSpace X) {x y : X} (h : x ≠ y) : 0 < M.dist x y := sorry'
reference: |-
  theorem MetricSpace.dist_pos {X : Type u} (M : MetricSpace X) {x y : X} (h : x ≠ y) : 0 < M.dist x y
      := by
    rcases lt_or_eq_of_le (MetricSpace.nonneg M x y) with hlt | heq
    · exact hlt
    · exact absurd (M.dist_eq_zero.mp heq.symm) h
spec: 'example : ∀ {X : Type u} (M : MetricSpace X) {x y : X} (h : x ≠ y), 0 < M.dist x y := _root_.GeneralTopology.MetricSpace.dist_pos'
preamble: |
  universe u v
  namespace GeneralTopology
  structure MetricSpace (X : Type u) where
    dist : X → X → ℝ
    dist_comm : ∀ {x y}, dist x y = dist y x
    dist_triangle : ∀ x y z, dist x z ≤ dist x y + dist y z
    dist_eq_zero : ∀ {x y}, dist x y = 0 ↔ x = y

  theorem MetricSpace.dist_self {X : Type u} (M : MetricSpace X) {x : X} : M.dist x x = 0 :=
    M.dist_eq_zero.mpr rfl

  theorem MetricSpace.nonneg {X : Type u} (M : MetricSpace X) (x y : X) : 0 ≤ M.dist x y := by
    have h := M.dist_triangle x y x
    rw [M.dist_self] at h
    have e : M.dist y x = M.dist x y := M.dist_comm
    rw [e] at h
    linarith

  end GeneralTopology
  open GeneralTopology
description: Show that `0 < M.dist x y` when `x ≠ y`. Split the non-negativity with `lt_or_eq_of_le`; in the second case `M.dist_eq_zero` turns the equation into `x = y`, which the hypothesis refutes.
stub_names:
- MetricSpace.dist_pos
namespace: GeneralTopology
seq: 144
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/Separation.lean
  line: 98
  closure_rounds: 1
  check_kind: theorem
