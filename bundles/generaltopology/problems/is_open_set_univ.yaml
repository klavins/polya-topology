slug: is_open_set_univ
kind: theorem
title: The Whole Space is Open
statement: Every point of the whole space has room around it, since every ball is inside the whole space whatever its radius. Any positive number will serve; `1` is as good as another.
concepts:
- open_sets
stub: 'theorem Metric.isOpenSet_univ {X : Type u} (M : Metric X) : M.IsOpenSet (Set.univ : Set X) := sorry'
reference: |-
  theorem Metric.isOpenSet_univ {X : Type u} (M : Metric X) : M.IsOpenSet (Set.univ : Set X) :=
    fun _ _ => ⟨1, one_pos, fun _ _ => trivial⟩
spec: 'example : ∀ {X : Type u} (M : Metric X), M.IsOpenSet (Set.univ : Set X) := _root_.GeneralTopology.Metric.isOpenSet_univ'
preamble: |
  universe u
  namespace GeneralTopology
  structure Metric (X : Type u) where
    dist : X → X → ℝ
    symm : ∀ {x y}, dist x y = dist y x
    triangle : ∀ x y z, dist x z ≤ dist x y + dist y z
    eq_zero : ∀ {x y}, dist x y = 0 ↔ x = y

  def Metric.ball {X : Type u} (M : Metric X) (x : X) (ε : ℝ) : Set X :=
    {y | M.dist x y < ε}

  def Metric.IsOpenSet {X : Type u} (M : Metric X) (U : Set X) : Prop :=
    ∀ x ∈ U, ∃ ε > 0, M.ball x ε ⊆ U

  end GeneralTopology
  open GeneralTopology
description: Show that `Set.univ` is open in any metric space. The witness is `⟨1, one_pos, _⟩`, and the containment holds because everything belongs to `Set.univ`, which `trivial` proves.
stub_names:
- Metric.isOpenSet_univ
namespace: GeneralTopology
seq: 33
provenance:
  license: CC-BY-SA-4.0
  source: extract@5060c35
  file: PolyaTopology/GeneralTopology/TopologicalSpaces.lean
  line: 68
  closure_rounds: 1
  check_kind: theorem
