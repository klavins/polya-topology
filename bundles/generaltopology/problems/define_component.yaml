slug: define_component
kind: example
title: The Component of a Point
statement: |-
  The *connected component* of `x` is the union of every connected set that contains `x`. All of those sets pass through `x`, so the union is connected by the problem just proved, and nothing connected containing `x` lies outside it: it is the largest connected set around `x`.

  The point itself belongs to it, since `{x}` is one of the sets united.
concepts:
- components
stub: |-
  def TopologicalSpace.connectedComponent {X : Type u} (T : TopologicalSpace X) (x : X) : Set X := sorry

  theorem TopologicalSpace.mem_component {X : Type u} (T : TopologicalSpace X) (x : X)
      : x ∈ T.connectedComponent x := sorry
reference: |-
  def TopologicalSpace.connectedComponent {X : Type u} (T : TopologicalSpace X) (x : X) : Set X :=
    ⋃₀ {S | T.IsPreconnected S ∧ x ∈ S}

  theorem TopologicalSpace.mem_component {X : Type u} (T : TopologicalSpace X) (x : X)
      : x ∈ T.connectedComponent x :=
    ⟨{x}, ⟨TopologicalSpace.isPreconnected_singleton T x, rfl⟩, rfl⟩
spec: |-
  example {X : Type u} (T : TopologicalSpace X) (x : X) : _root_.GeneralTopology.TopologicalSpace.connectedComponent T x = (⋃₀ {S | T.IsPreconnected S ∧ x ∈ S} : Set X) := rfl
  example : ∀ {X : Type u} (T : TopologicalSpace X) (x : X), x ∈ T.connectedComponent x := _root_.GeneralTopology.TopologicalSpace.mem_component
  example (X : Type) (T : TopologicalSpace X) (x y : X) :
      y ∈ T.connectedComponent x ↔ ∃ S, (T.IsPreconnected S ∧ x ∈ S) ∧ y ∈ S := Iff.rfl
preamble: |
  universe u v
  namespace GeneralTopology
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  def TopologicalSpace.IsPreconnected {X : Type u} (T : TopologicalSpace X) (S : Set X) : Prop :=
    ∀ U V, T.IsOpen U → T.IsOpen V → (∀ x ∈ S, x ∈ U → x ∉ V) → S ⊆ U ∪ V → S ⊆ U ∨ S ⊆ V

  theorem TopologicalSpace.isPreconnected_singleton {X : Type u} (T : TopologicalSpace X) (x : X) :
      T.IsPreconnected {x} := by
    intro U V _ _ _ hcov
    have hx : x ∈ ({x} : Set X) := rfl
    rcases hcov hx with h | h
    · refine Or.inl fun y hy => ?_
      have e : y = x := hy
      rw [e]
      exact h
    · refine Or.inr fun y hy => ?_
      have e : y = x := hy
      rw [e]
      exact h

  end GeneralTopology
  open GeneralTopology
description: Define `TopologicalSpace.connectedComponent`, the union `⋃₀ {S | T.IsPreconnected S ∧ x ∈ S}`, and prove `TopologicalSpace.mem_component`. The witness for the second is the singleton, whose connectedness is the previous problem and whose two memberships are `rfl`.
stub_names:
- TopologicalSpace.connectedComponent
- TopologicalSpace.mem_component
namespace: GeneralTopology
seq: 173
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/Connectedness.lean
  line: 449
  closure_rounds: 1
  check_kind: mixed
