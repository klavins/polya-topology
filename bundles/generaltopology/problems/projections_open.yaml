slug: projections_open
kind: example
title: The Projections Are Open Maps
statement: |-
  A continuous map need not carry open sets to open sets. The projections do.

  Let `W` be open in the product and let `p` be a point of it. Some open box `U ×ˢ V` around `p` lies inside `W`. Then `U` lies inside the image of `W` under the first projection, because a point `u` of `U` is the first coordinate of `(u, p.2)`, which is in the box and so in `W`. So the image has an open set around each of its points, which is openness.
concepts:
- product_maps
stub: |-
  theorem Topology.isOpenMap_fst {X : Type u} {Y : Type v} (T : Topology X) (T' : Topology Y) :
      (T.prod T').IsOpenMap T Prod.fst := sorry

  theorem Topology.isOpenMap_snd {X : Type u} {Y : Type v} (T : Topology X) (T' : Topology Y) :
      (T.prod T').IsOpenMap T' Prod.snd := sorry
reference: |-
  theorem Topology.isOpenMap_fst {X : Type u} {Y : Type v} (T : Topology X) (T' : Topology Y) :
      (T.prod T').IsOpenMap T Prod.fst := by
    intro W hW
    rw [Topology.isOpen_iff_nbhd]
    rintro x ⟨p, hpW, rfl⟩
    obtain ⟨U, V, hU, hV, hpUV, hsub⟩ := hW p hpW
    exact ⟨U, hU, hpUV.1, fun u hu => ⟨(u, p.2), hsub ⟨hu, hpUV.2⟩, rfl⟩⟩

  theorem Topology.isOpenMap_snd {X : Type u} {Y : Type v} (T : Topology X) (T' : Topology Y) :
      (T.prod T').IsOpenMap T' Prod.snd := by
    intro W hW
    rw [Topology.isOpen_iff_nbhd]
    rintro y ⟨p, hpW, rfl⟩
    obtain ⟨U, V, hU, hV, hpUV, hsub⟩ := hW p hpW
    exact ⟨V, hV, hpUV.2, fun v hv => ⟨(p.1, v), hsub ⟨hpUV.1, hv⟩, rfl⟩⟩
spec: |-
  example : ∀ {X : Type u} {Y : Type v} (T : Topology X) (T' : Topology Y), (T.prod T').IsOpenMap T Prod.fst := _root_.GeneralTopology.Topology.isOpenMap_fst
  example : ∀ {X : Type u} {Y : Type v} (T : Topology X) (T' : Topology Y), (T.prod T').IsOpenMap T' Prod.snd := _root_.GeneralTopology.Topology.isOpenMap_snd
preamble: |
  universe u v w
  namespace GeneralTopology
  structure Topology (X : Type u) where
    IsOpen : Set X → Prop
    univ : IsOpen Set.univ
    inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  def Topology.IsNbhd {X : Type u} (T : Topology X) (x : X) (N : Set X) : Prop :=
    ∃ U, T.IsOpen U ∧ x ∈ U ∧ U ⊆ N

  theorem Topology.nbhd_of_isOpen {X : Type u} (T : Topology X) {U : Set X}
      (hU : T.IsOpen U) {x : X} (hx : x ∈ U) : T.IsNbhd x U :=
    ⟨U, hU, hx, subset_rfl⟩

  theorem Topology.union_of_opens_inside {X : Type u} (T : Topology X) (U : Set X)
      (h : ∀ x ∈ U, T.IsNbhd x U) : U = ⋃₀ {V | T.IsOpen V ∧ V ⊆ U} := by
    apply Set.Subset.antisymm
    · intro x hx
      obtain ⟨V, hV, hxV, hVU⟩ := h x hx
      exact ⟨V, ⟨hV, hVU⟩, hxV⟩
    · rintro x ⟨V, ⟨_, hVU⟩, hxV⟩
      exact hVU hxV

  theorem Topology.isOpen_iff_nbhd {X : Type u} (T : Topology X) (U : Set X) :
      T.IsOpen U ↔ ∀ x ∈ U, T.IsNbhd x U := by
    constructor
    · intro hU x hx
      exact T.nbhd_of_isOpen hU hx
    · intro h
      rw [T.union_of_opens_inside U h]
      exact T.sUnion (fun V hV => hV.1)

  def Topology.IsOpenMap {X : Type u} {Y : Type v} (T : Topology X) (T' : Topology Y)
      (f : X → Y) : Prop :=
    ∀ U, T.IsOpen U → T'.IsOpen (f '' U)

  def Topology.prod {X : Type u} {Y : Type v} (T : Topology X) (T' : Topology Y) :
      Topology (X × Y) where
    IsOpen := fun W => ∀ p ∈ W, ∃ U V, T.IsOpen U ∧ T'.IsOpen V ∧ p ∈ U ×ˢ V ∧ U ×ˢ V ⊆ W
    univ := fun _ _ => ⟨Set.univ, Set.univ, T.univ, T'.univ, ⟨trivial, trivial⟩, fun _ _ => trivial⟩
    inter := by
      rintro W W' hW hW' p hp
      obtain ⟨U, V, hU, hV, hpUV, hsub⟩ := hW p hp.1
      obtain ⟨U', V', hU', hV', hpUV', hsub'⟩ := hW' p hp.2
      refine ⟨U ∩ U', V ∩ V', T.inter hU hU', T'.inter hV hV',
        ⟨⟨hpUV.1, hpUV'.1⟩, ⟨hpUV.2, hpUV'.2⟩⟩, ?_⟩
      rintro q ⟨hq1, hq2⟩
      exact ⟨hsub ⟨hq1.1, hq2.1⟩, hsub' ⟨hq1.2, hq2.2⟩⟩
    sUnion := by
      rintro F h p ⟨W, hWF, hpW⟩
      obtain ⟨U, V, hU, hV, hpUV, hsub⟩ := h W hWF p hpW
      exact ⟨U, V, hU, hV, hpUV, fun q hq => ⟨W, hWF, hsub hq⟩⟩

  end GeneralTopology
  open GeneralTopology
description: Show that `Prod.fst` and `Prod.snd` are open maps from `T.prod T'`. `Topology.isOpen_iff_nbhd` turns the goal into a statement at each point of the image; take that point apart with `rintro x ⟨p, hpW, rfl⟩`, offer the box's side, and name `(u, p.2)` as the point a given `u` comes from.
stub_names:
- Topology.isOpenMap_fst
- Topology.isOpenMap_snd
namespace: GeneralTopology
seq: 116
provenance:
  license: CC-BY-SA-4.0
  source: extract@9c4b8d6
  file: PolyaTopology/GeneralTopology/Products.lean
  line: 321
  closure_rounds: 1
  check_kind: theorem
