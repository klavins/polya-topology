slug: projections_open
kind: example
title: The Projections Are Open Maps
statement: |-
  A continuous map need not carry open sets to open sets. The projections do.

  Let `W` be open in the product and let `p` be a point of it. Some open box `U ×ˢ V` around `p` lies inside `W`. Then `U` lies inside the image of `W` under the first projection, because a point `u` of `U` is the first coordinate of `(u, p.2)`, which is in the box and so in `W`. So the image has an open set around each of its points, which is openness.
concepts:
- product_maps
stub: |-
  theorem TopologicalSpace.isOpenMap_fst {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y) :
      (T.prod T').IsOpenMap T Prod.fst := sorry

  theorem TopologicalSpace.isOpenMap_snd {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y) :
      (T.prod T').IsOpenMap T' Prod.snd := sorry
reference: |-
  theorem TopologicalSpace.isOpenMap_fst {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y) :
      (T.prod T').IsOpenMap T Prod.fst := by
    intro W hW
    rw [TopologicalSpace.isOpen_iff_nbhd]
    rintro x ⟨p, hpW, rfl⟩
    obtain ⟨U, V, hU, hV, hpUV, hsub⟩ := hW p hpW
    exact ⟨U, hU, hpUV.1, fun u hu => ⟨(u, p.2), hsub ⟨hu, hpUV.2⟩, rfl⟩⟩

  theorem TopologicalSpace.isOpenMap_snd {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y) :
      (T.prod T').IsOpenMap T' Prod.snd := by
    intro W hW
    rw [TopologicalSpace.isOpen_iff_nbhd]
    rintro y ⟨p, hpW, rfl⟩
    obtain ⟨U, V, hU, hV, hpUV, hsub⟩ := hW p hpW
    exact ⟨V, hV, hpUV.2, fun v hv => ⟨(p.1, v), hsub ⟨hpUV.1, hv⟩, rfl⟩⟩
spec: |-
  example : ∀ {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y), (T.prod T').IsOpenMap T Prod.fst := _root_.GeneralTopology.TopologicalSpace.isOpenMap_fst
  example : ∀ {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y), (T.prod T').IsOpenMap T' Prod.snd := _root_.GeneralTopology.TopologicalSpace.isOpenMap_snd
preamble: |
  universe u v w
  namespace GeneralTopology
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  def TopologicalSpace.IsNbhd {X : Type u} (T : TopologicalSpace X) (x : X) (N : Set X) : Prop :=
    ∃ U, T.IsOpen U ∧ x ∈ U ∧ U ⊆ N

  theorem TopologicalSpace.nbhd_of_isOpen {X : Type u} (T : TopologicalSpace X) {U : Set X}
      (hU : T.IsOpen U) {x : X} (hx : x ∈ U) : T.IsNbhd x U :=
    ⟨U, hU, hx, subset_rfl⟩

  theorem TopologicalSpace.union_of_opens_inside {X : Type u} (T : TopologicalSpace X) (U : Set X)
      (h : ∀ x ∈ U, T.IsNbhd x U) : U = ⋃₀ {V | T.IsOpen V ∧ V ⊆ U} := by
    apply Set.Subset.antisymm
    · intro x hx
      obtain ⟨V, hV, hxV, hVU⟩ := h x hx
      exact ⟨V, ⟨hV, hVU⟩, hxV⟩
    · rintro x ⟨V, ⟨_, hVU⟩, hxV⟩
      exact hVU hxV

  theorem TopologicalSpace.isOpen_iff_nbhd {X : Type u} (T : TopologicalSpace X) (U : Set X) :
      T.IsOpen U ↔ ∀ x ∈ U, T.IsNbhd x U := by
    constructor
    · intro hU x hx
      exact T.nbhd_of_isOpen hU hx
    · intro h
      rw [T.union_of_opens_inside U h]
      exact T.isOpen_sUnion (fun V hV => hV.1)

  def TopologicalSpace.IsOpenMap {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y)
      (f : X → Y) : Prop :=
    ∀ U, T.IsOpen U → T'.IsOpen (f '' U)

  def TopologicalSpace.prod {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y) :
      TopologicalSpace (X × Y) where
    IsOpen := fun W => ∀ p ∈ W, ∃ U V, T.IsOpen U ∧ T'.IsOpen V ∧ p ∈ U ×ˢ V ∧ U ×ˢ V ⊆ W
    isOpen_univ := fun _ _ =>
      ⟨Set.univ, Set.univ, T.isOpen_univ, T'.isOpen_univ, ⟨trivial, trivial⟩, fun _ _ => trivial⟩
    isOpen_inter := by
      rintro W W' hW hW' p hp
      obtain ⟨U, V, hU, hV, hpUV, hsub⟩ := hW p hp.1
      obtain ⟨U', V', hU', hV', hpUV', hsub'⟩ := hW' p hp.2
      refine ⟨U ∩ U', V ∩ V', T.isOpen_inter hU hU', T'.isOpen_inter hV hV',
        ⟨⟨hpUV.1, hpUV'.1⟩, ⟨hpUV.2, hpUV'.2⟩⟩, ?_⟩
      rintro q ⟨hq1, hq2⟩
      exact ⟨hsub ⟨hq1.1, hq2.1⟩, hsub' ⟨hq1.2, hq2.2⟩⟩
    isOpen_sUnion := by
      rintro F h p ⟨W, hWF, hpW⟩
      obtain ⟨U, V, hU, hV, hpUV, hsub⟩ := h W hWF p hpW
      exact ⟨U, V, hU, hV, hpUV, fun q hq => ⟨W, hWF, hsub hq⟩⟩

  end GeneralTopology
  open GeneralTopology
description: Show that `Prod.fst` and `Prod.snd` are open maps from `T.prod T'`. `TopologicalSpace.isOpen_iff_nbhd` turns the goal into a statement at each point of the image; take that point apart with `rintro x ⟨p, hpW, rfl⟩`, offer the box's side, and name `(u, p.2)` as the point a given `u` comes from.
stub_names:
- TopologicalSpace.isOpenMap_fst
- TopologicalSpace.isOpenMap_snd
namespace: GeneralTopology
seq: 116
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/Products.lean
  line: 333
  closure_rounds: 1
  check_kind: theorem
