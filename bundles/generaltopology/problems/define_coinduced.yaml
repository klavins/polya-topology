slug: define_coinduced
kind: definition
title: The Topology Coinduced by a Map
statement: |-
  Let `f` carry a space `X` to a set `Y`. Call a subset of `Y` open when its preimage under `f` is open in `X`. The three conditions then hold for the reason just proved: a preimage commutes with each of the three operations, so each condition is the corresponding condition upstairs.

  Written this way the definition is shorter than the induced one, which had to say "is the preimage of an open set" and carry an existential to say it. Here there is no existential, because the test is applied to the set itself.
concepts:
- final_topology
stub: |-
  def TopologicalSpace.coinduced {X : Type u} {Y : Type v} (T : TopologicalSpace X) (f : X → Y)
      : TopologicalSpace Y := sorry
reference: |-
  def TopologicalSpace.coinduced {X : Type u} {Y : Type v} (T : TopologicalSpace X) (f : X → Y)
      : TopologicalSpace Y where
    IsOpen := fun V => T.IsOpen (f ⁻¹' V)
    isOpen_univ := by
      rw [Set.preimage_univ]
      exact T.isOpen_univ
    isOpen_inter := by
      intro V W hV hW
      rw [Set.preimage_inter]
      exact T.isOpen_inter hV hW
    isOpen_sUnion := by
      intro F h
      rw [preimage_sUnion_image]
      refine T.isOpen_sUnion ?_
      rintro W ⟨V, hVF, rfl⟩
      exact h V hVF
spec: |-
  example (X Y : Type) (T : TopologicalSpace X) (f : X → Y) (V : Set Y) :
      (T.coinduced f).IsOpen V ↔ T.IsOpen (f ⁻¹' V) := Iff.rfl
preamble: |
  universe u v w
  namespace GeneralTopology
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  theorem preimage_sUnion_image {X : Type u} {Y : Type v} (f : X → Y) (F : Set (Set Y)) :
      f ⁻¹' (⋃₀ F) = ⋃₀ (Set.preimage f '' F) := by
    apply Set.Subset.antisymm
    · rintro x ⟨V, hVF, hxV⟩
      exact ⟨f ⁻¹' V, ⟨V, hVF, rfl⟩, hxV⟩
    · rintro x ⟨W, ⟨V, hVF, rfl⟩, hxW⟩
      exact ⟨V, hVF, hxW⟩

  end GeneralTopology
  open GeneralTopology
description: Define `TopologicalSpace.coinduced`, the topology on `Y` whose `IsOpen V` says `T.IsOpen (f ⁻¹' V)`. `Set.preimage_univ` and `Set.preimage_inter` rewrite the first two fields; the third rewrites by the previous problem and then applies the union condition of `T` to the collection of preimages.
stub_names:
- TopologicalSpace.coinduced
namespace: GeneralTopology
seq: 123
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/Quotients.lean
  line: 42
  closure_rounds: 1
  check_kind: definition
