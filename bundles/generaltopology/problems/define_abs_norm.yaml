slug: define_abs_norm
kind: example
title: The Absolute Value as a Norm
statement: |-
  The real numbers are themselves a real vector space of dimension one, and the length of a number is its absolute value. The three conditions are standard facts again: `abs_eq_zero` for non-degeneracy, `abs_mul` for homogeneity once `smul_eq_mul` has said that scaling a real number is multiplying it, and `abs_add_le` for the triangle inequality.

  This is the Euclidean length in dimension one, the square root of `x²` being `|x|`.

  The construction of the previous problem should now return the metric this section began with, and it does: the distance it produces is `|x - y|`, which is what `line` was defined to be.
concepts:
- norm_metric
stub: |-
  def absNorm : Norm ℝ := sorry

  theorem absNorm_toMetric : absNorm.toMetric = line := sorry
reference: |-
  def absNorm : Norm ℝ where
    norm x := |x|
    eq_zero {x} := abs_eq_zero
    smul c x := by rw [smul_eq_mul, abs_mul]
    triangle x y := abs_add_le x y

  theorem absNorm_toMetric : absNorm.toMetric = line := rfl
spec: |-
  example : _root_.GeneralTopology.absNorm.toMetric = line := _root_.GeneralTopology.absNorm_toMetric
  example (x : ℝ) : _root_.GeneralTopology.absNorm.norm x = |x| := rfl
preamble: |
  universe u
  namespace GeneralTopology
  structure Metric (X : Type u) where
    dist : X → X → ℝ
    symm : ∀ {x y}, dist x y = dist y x
    triangle : ∀ x y z, dist x z ≤ dist x y + dist y z
    eq_zero : ∀ {x y}, dist x y = 0 ↔ x = y

  def line : Metric ℝ where
    dist x y := |x - y|
    symm {x y} := abs_sub_comm x y
    triangle x y z := abs_sub_le x y z
    eq_zero {x y} := by rw [abs_eq_zero, sub_eq_zero]

  structure Norm (V : Type u) [AddCommGroup V] [Module ℝ V] where
    norm : V → ℝ
    eq_zero : ∀ {v}, norm v = 0 ↔ v = 0
    smul : ∀ (c : ℝ) (v : V), norm (c • v) = |c| * norm v
    triangle : ∀ v w, norm (v + w) ≤ norm v + norm w

  theorem Norm.neg {V : Type u} [AddCommGroup V] [Module ℝ V] (N : Norm V) (v : V) :
      N.norm (-v) = N.norm v := by
    have h := N.smul (-1) v
    rw [neg_one_smul] at h
    rw [h, abs_neg, abs_one, one_mul]

  theorem Norm.sub_comm {V : Type u} [AddCommGroup V] [Module ℝ V] (N : Norm V) (v w : V) :
      N.norm (v - w) = N.norm (w - v) := by
    rw [← N.neg (v - w), neg_sub]

  def Norm.toMetric {V : Type u} [AddCommGroup V] [Module ℝ V] (N : Norm V) : Metric V where
    dist v w := N.norm (v - w)
    symm {v w} := N.sub_comm v w
    triangle u v w := by
      have h := N.triangle (u - v) (v - w)
      rw [sub_add_sub_cancel] at h
      exact h
    eq_zero {v w} := by rw [N.eq_zero, sub_eq_zero]

  end GeneralTopology
  open GeneralTopology
description: Define `absNorm`, the absolute value as a norm on `ℝ`, and show that the metric it induces is `line`. The three fields are the facts named above, the second after rewriting with `smul_eq_mul` and `abs_mul`; the two metrics are the same by `rfl`.
stub_names:
- absNorm
- absNorm_toMetric
namespace: GeneralTopology
seq: 19
provenance:
  license: CC-BY-SA-4.0
  source: extract@8c3087a
  file: PolyaTopology/GeneralTopology/MetricSpaces.lean
  line: 495
  closure_rounds: 1
  check_kind: mixed
