slug: t1_iff_points_closed
kind: theorem
title: T₁ Means Every Point is Closed
statement: |-
  The axiom T₁ has a one-word form: the points of the space are closed sets.

  If the points are closed then, given `x ≠ y`, the set `{y}ᶜ` is open, it contains `x`, and it leaves `y` out — which is the axiom.

  Conversely, suppose the axiom holds, and look at `{x}ᶜ`. Each of its points `y` is distinct from `x`, so the axiom supplies an open set holding `y` and missing `x`; an open set missing `x` lies inside `{x}ᶜ`. So `{x}ᶜ` is a neighbourhood of each of its own points, and is therefore open.
concepts:
- separation_closures
stub: |-
  theorem TopologicalSpace.isT1_iff_isClosed_singleton {X : Type u} (T : TopologicalSpace X) :
      T.IsT1 ↔ ∀ x : X, T.IsClosed {x} := sorry
reference: |-
  theorem TopologicalSpace.isT1_iff_isClosed_singleton {X : Type u} (T : TopologicalSpace X) :
      T.IsT1 ↔ ∀ x : X, T.IsClosed {x} := by
    constructor
    · intro h x
      show T.IsOpen _
      rw [TopologicalSpace.isOpen_iff_nbhd]
      intro y hy
      obtain ⟨U, hU, hyU, hxU⟩ := h y x hy
      exact ⟨U, hU, hyU, fun z hz he => hxU (by rw [← show z = x from he]; exact hz)⟩
    · intro h x y hxy
      exact ⟨{y}ᶜ, h y, hxy, fun hn => hn rfl⟩
spec: 'example : ∀ {X : Type u} (T : TopologicalSpace X), T.IsT1 ↔ ∀ x : X, T.IsClosed {x} := _root_.GeneralTopology.TopologicalSpace.isT1_iff_isClosed_singleton'
preamble: |
  universe u v
  namespace GeneralTopology
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  def TopologicalSpace.IsClosed {X : Type u} (T : TopologicalSpace X) (C : Set X) : Prop
      := T.IsOpen Cᶜ

  def TopologicalSpace.IsNbhd {X : Type u} (T : TopologicalSpace X) (x : X) (N : Set X) : Prop :=
    ∃ U, T.IsOpen U ∧ x ∈ U ∧ U ⊆ N

  theorem TopologicalSpace.nbhd_of_isOpen {X : Type u} (T : TopologicalSpace X) {U : Set X}
      (hU : T.IsOpen U) {x : X} (hx : x ∈ U) : T.IsNbhd x U :=
    ⟨U, hU, hx, subset_rfl⟩

  theorem TopologicalSpace.union_of_opens_inside {X : Type u} (T : TopologicalSpace X) (U : Set X)
      (h : ∀ x ∈ U, T.IsNbhd x U) : U = ⋃₀ {V | T.IsOpen V ∧ V ⊆ U} := by
    apply Set.Subset.antisymm
    · intro x hx
      obtain ⟨V, hV, hxV, hVU⟩ := h x hx
      exact ⟨V, ⟨hV, hVU⟩, hxV⟩
    · rintro x ⟨V, ⟨_, hVU⟩, hxV⟩
      exact hVU hxV

  theorem TopologicalSpace.isOpen_iff_nbhd {X : Type u} (T : TopologicalSpace X) (U : Set X) :
      T.IsOpen U ↔ ∀ x ∈ U, T.IsNbhd x U := by
    constructor
    · intro hU x hx
      exact T.nbhd_of_isOpen hU hx
    · intro h
      rw [T.union_of_opens_inside U h]
      exact T.isOpen_sUnion (fun V hV => hV.1)

  def TopologicalSpace.IsT1 {X : Type u} (T : TopologicalSpace X) : Prop :=
    ∀ x y : X, x ≠ y → ∃ U, T.IsOpen U ∧ x ∈ U ∧ y ∉ U

  end GeneralTopology
  open GeneralTopology
description: Prove `TopologicalSpace.isT1_iff_isClosed_singleton`. Forwards, restate the closedness with `show` and turn it into a statement at each point with `TopologicalSpace.isOpen_iff_nbhd`. A membership `y ∈ {x}` is the equation `y = x`, so a point of `{x}ᶜ` arrives with exactly the hypothesis `y ≠ x` the axiom wants.
stub_names:
- TopologicalSpace.isT1_iff_isClosed_singleton
namespace: GeneralTopology
seq: 150
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/Separation.lean
  line: 277
  closure_rounds: 1
  check_kind: theorem
