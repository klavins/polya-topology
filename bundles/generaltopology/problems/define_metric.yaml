slug: define_metric
kind: definition
title: The Definition of a Metric Space
statement: |-
  What must a rule satisfy to deserve the name of a distance? Three things, and each of them holds for distance on a map.

  *Symmetry*: the distance from `x` to `y` is the distance from `y` to `x`. A distance belongs to an unordered pair, not to a journey with a direction.

  *The triangle inequality*: going from `x` to `z` by way of `y` is never shorter than going directly, so `d x z ≤ d x y + d y z`. In the plane: one side of a triangle is at most the sum of the other two.

  *Non-degeneracy*: `d x y = 0` exactly when `x = y`.

  A structure gathers data and the conditions on it under one name, so a term of type `MetricSpace X` is a distance function with proofs of all three. Braces mark a variable Lean is to infer, so a field stated `∀ {x y}, …` is used as just `M.dist_comm`.
concepts:
- metric_space
stub: |-
  structure MetricSpace (X : Type u) where
    dist : sorry
    dist_comm : sorry
    dist_triangle : sorry
    dist_eq_zero : sorry
reference: |-
  structure MetricSpace (X : Type u) where
    dist : X → X → ℝ
    dist_comm : ∀ {x y}, dist x y = dist y x
    dist_triangle : ∀ x y z, dist x z ≤ dist x y + dist y z
    dist_eq_zero : ∀ {x y}, dist x y = 0 ↔ x = y
spec: |-
  example (X : Type) (M : _root_.GeneralTopology.MetricSpace X) (x y z : X) :
      M.dist x y = M.dist y x
        ∧ M.dist x z ≤ M.dist x y + M.dist y z
        ∧ (M.dist x y = 0 ↔ x = y) :=
    ⟨by apply M.dist_comm, by apply M.dist_triangle, by simp [M.dist_eq_zero]⟩
preamble: |
  universe u
description: 'Define the structure `MetricSpace X` for a type `X`, with four fields in this order: `dist`, of type `X → X → ℝ`; then `symm`, `triangle` and `eq_zero`, carrying the three conditions above. Bind the points implicitly in `symm` and `eq_zero`, explicitly in `triangle`, whose middle point is never fixed by the goal.'
stub_names:
- MetricSpace
namespace: GeneralTopology
seq: 0
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/MetricSpaces.lean
  line: 23
  closure_rounds: 1
  check_kind: definition
