slug: sum_universal
kind: example
title: A Map Out of a Sum is a Pair of Maps
statement: |-
  To give a map from `X ⊕ Y` into `Z` is to give a map from `X` and a map from `Y`, since a point of the sum is one or the other. `Sum.elim f g` is that map: it applies `f` on the first copy and `g` on the second.

  The topology was chosen so that the same holds of continuity, and again the proof is nothing. The preimage of a set under `Sum.elim f g`, read on the first copy, is its preimage under `f`, and read on the second copy, its preimage under `g`.
concepts:
- sum_space
stub: |-
  theorem TopologicalSpace.continuous_sum_elim {X : Type u} {Y : Type v} {Z : Type w}
      {T : TopologicalSpace X}
      {T' : TopologicalSpace Y} (S : TopologicalSpace Z) {f : X → Z} {g : Y → Z}
      (hf : T.Continuous S f)
      (hg : T'.Continuous S g) : (T.sum T').Continuous S (Sum.elim f g) := sorry

  theorem TopologicalSpace.continuous_sum_iff {X : Type u} {Y : Type v} {Z : Type w}
      (T : TopologicalSpace X)
      (T' : TopologicalSpace Y) (S : TopologicalSpace Z) (h : X ⊕ Y → Z) :
      (T.sum T').Continuous S h ↔
        T.Continuous S (h ∘ Sum.inl) ∧ T'.Continuous S (h ∘ Sum.inr) := sorry
reference: |-
  theorem TopologicalSpace.continuous_sum_elim {X : Type u} {Y : Type v} {Z : Type w}
      {T : TopologicalSpace X}
      {T' : TopologicalSpace Y} (S : TopologicalSpace Z) {f : X → Z} {g : Y → Z}
      (hf : T.Continuous S f)
      (hg : T'.Continuous S g) : (T.sum T').Continuous S (Sum.elim f g) :=
    fun W hW => ⟨hf W hW, hg W hW⟩

  theorem TopologicalSpace.continuous_sum_iff {X : Type u} {Y : Type v} {Z : Type w}
      (T : TopologicalSpace X)
      (T' : TopologicalSpace Y) (S : TopologicalSpace Z) (h : X ⊕ Y → Z) :
      (T.sum T').Continuous S h ↔
        T.Continuous S (h ∘ Sum.inl) ∧ T'.Continuous S (h ∘ Sum.inr) := by
    constructor
    · intro hh
      exact ⟨fun W hW => (hh W hW).1, fun W hW => (hh W hW).2⟩
    · rintro ⟨hf, hg⟩ W hW
      exact ⟨hf W hW, hg W hW⟩
spec: |-
  example : ∀ {X : Type u} {Y : Type v} {Z : Type w}
      {T : TopologicalSpace X}
      {T' : TopologicalSpace Y} (S : TopologicalSpace Z) {f : X → Z} {g : Y → Z}
      (hf : T.Continuous S f)
      (hg : T'.Continuous S g), (T.sum T').Continuous S (Sum.elim f g) := _root_.GeneralTopology.TopologicalSpace.continuous_sum_elim
  example : ∀ {X : Type u} {Y : Type v} {Z : Type w}
      (T : TopologicalSpace X)
      (T' : TopologicalSpace Y) (S : TopologicalSpace Z) (h : X ⊕ Y → Z), (T.sum T').Continuous S h ↔
        T.Continuous S (h ∘ Sum.inl) ∧ T'.Continuous S (h ∘ Sum.inr) := _root_.GeneralTopology.TopologicalSpace.continuous_sum_iff
preamble: |
  universe u v w
  namespace GeneralTopology
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  def TopologicalSpace.Continuous {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y)
      (f : X → Y) : Prop :=
    ∀ U, T'.IsOpen U → T.IsOpen (f ⁻¹' U)

  theorem preimage_sUnion_image {X : Type u} {Y : Type v} (f : X → Y) (F : Set (Set Y)) :
      f ⁻¹' (⋃₀ F) = ⋃₀ (Set.preimage f '' F) := by
    apply Set.Subset.antisymm
    · rintro x ⟨V, hVF, hxV⟩
      exact ⟨f ⁻¹' V, ⟨V, hVF, rfl⟩, hxV⟩
    · rintro x ⟨W, ⟨V, hVF, rfl⟩, hxW⟩
      exact ⟨V, hVF, hxW⟩

  def TopologicalSpace.sum {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y) :
      TopologicalSpace (X ⊕ Y) where
    IsOpen := fun W => T.IsOpen (Sum.inl ⁻¹' W) ∧ T'.IsOpen (Sum.inr ⁻¹' W)
    isOpen_univ := by
      rw [Set.preimage_univ, Set.preimage_univ]
      exact ⟨T.isOpen_univ, T'.isOpen_univ⟩
    isOpen_inter := by
      intro V W hV hW
      rw [Set.preimage_inter, Set.preimage_inter]
      exact ⟨T.isOpen_inter hV.1 hW.1, T'.isOpen_inter hV.2 hW.2⟩
    isOpen_sUnion := by
      intro F h
      rw [preimage_sUnion_image, preimage_sUnion_image]
      constructor
      · exact T.isOpen_sUnion (by rintro W ⟨V, hVF, rfl⟩; exact (h V hVF).1)
      · exact T'.isOpen_sUnion (by rintro W ⟨V, hVF, rfl⟩; exact (h V hVF).2)

  end GeneralTopology
  open GeneralTopology
description: 'Prove that `Sum.elim f g` is continuous when `f` and `g` are, and then that a map `h : X ⊕ Y → Z` is continuous exactly when `h ∘ Sum.inl` and `h ∘ Sum.inr` are. Both are pairs of applications of the hypotheses, and neither needs any rewriting.'
stub_names:
- TopologicalSpace.continuous_sum_elim
- TopologicalSpace.continuous_sum_iff
namespace: GeneralTopology
seq: 137
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/Quotients.lean
  line: 439
  closure_rounds: 1
  check_kind: theorem
