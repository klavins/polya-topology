slug: ball_is_open
kind: theorem
title: An Open Ball is an Open Set
statement: |-
  The name would be a poor one if a ball were not open, so this is the first thing to check.

  Let `y` be a point of `M.ball x ε`, so `M.dist x y < ε`. The room left over is exactly `ε - M.dist x y`, and it is positive. A point `z` within that of `y` is within `ε` of `x`, since the trip from `x` to `z` may be made through `y`: `M.dist x z ≤ M.dist x y + M.dist y z < M.dist x y + (ε - M.dist x y)`.
concepts:
- open_sets
stub: |-
  theorem MetricSpace.isOpenSet_ball {X : Type u} (M : MetricSpace X) (x : X) (ε : ℝ) :
      M.IsOpenSet (M.ball x ε) := sorry
reference: |-
  theorem MetricSpace.isOpenSet_ball {X : Type u} (M : MetricSpace X) (x : X) (ε : ℝ) :
      M.IsOpenSet (M.ball x ε) := by
    intro y hy
    rw [M.mem_ball] at hy
    refine ⟨ε - M.dist x y, by linarith, ?_⟩
    intro z hz
    rw [M.mem_ball] at hz ⊢
    have h := M.dist_triangle x y z
    linarith
spec: 'example : ∀ {X : Type u} (M : MetricSpace X) (x : X) (ε : ℝ), M.IsOpenSet (M.ball x ε) := _root_.GeneralTopology.MetricSpace.isOpenSet_ball'
preamble: |
  universe u
  namespace GeneralTopology
  structure MetricSpace (X : Type u) where
    dist : X → X → ℝ
    dist_comm : ∀ {x y}, dist x y = dist y x
    dist_triangle : ∀ x y z, dist x z ≤ dist x y + dist y z
    dist_eq_zero : ∀ {x y}, dist x y = 0 ↔ x = y

  def MetricSpace.ball {X : Type u} (M : MetricSpace X) (x : X) (ε : ℝ) : Set X :=
    {y | M.dist x y < ε}

  theorem MetricSpace.mem_ball {X : Type u} (M : MetricSpace X) {x y : X} {ε : ℝ} :
      y ∈ M.ball x ε ↔ M.dist x y < ε := Iff.rfl

  def MetricSpace.IsOpenSet {X : Type u} (M : MetricSpace X) (U : Set X) : Prop :=
    ∀ x ∈ U, ∃ ε > 0, M.ball x ε ⊆ U

  end GeneralTopology
  open GeneralTopology
description: Show that `M.ball x ε` is open. Introduce a point of it and rewrite its membership as the inequality it is, offer `ε - M.dist x y` as the radius, and let the triangle inequality and `linarith` do the rest.
stub_names:
- MetricSpace.isOpenSet_ball
namespace: GeneralTopology
seq: 32
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/TopologicalSpaces.lean
  line: 46
  closure_rounds: 1
  check_kind: theorem
