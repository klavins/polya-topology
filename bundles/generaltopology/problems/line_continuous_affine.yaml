slug: line_continuous_affine
kind: theorem
title: An Affine Map of the Line
statement: |-
  The first map whose `δ` is not simply `ε`. Take `t ↦ a * t + b` on the line, with `a ≠ 0`.

  The two images differ by `a * (x - x')`, so their distance is `|a|` times the distance between the points. To hold that under `ε` it is enough to hold the distance between the points under `ε / |a|`, which is positive because `|a|` is.

  The slope is where the `δ` comes from, and a steeper map needs a smaller one. That is the whole of the exercise, and it is the reason the definition lets `δ` depend on `ε`.
concepts:
- epsilon_delta
stub: |-
  theorem line_continuous_affine (a b : ℝ) (ha : a ≠ 0) :
      line.Continuous line (fun t => a * t + b) := sorry
reference: |-
  theorem line_continuous_affine (a b : ℝ) (ha : a ≠ 0) :
      line.Continuous line (fun t => a * t + b) := by
    intro x ε hε
    have hpos : 0 < |a| := abs_pos.mpr ha
    refine ⟨ε / |a|, div_pos hε hpos, ?_⟩
    intro x' h
    have hx : |x - x'| < ε / |a| := h
    show |(a * x + b) - (a * x' + b)| < ε
    have e : (a * x + b) - (a * x' + b) = a * (x - x') := by ring
    rw [e, abs_mul]
    calc |a| * |x - x'| < |a| * (ε / |a|) := mul_lt_mul_of_pos_left hx hpos
      _ = ε := mul_div_cancel₀ ε (ne_of_gt hpos)
spec: 'example : ∀ (a b : ℝ) (ha : a ≠ 0), line.Continuous line (fun t => a * t + b) := _root_.GeneralTopology.line_continuous_affine'
preamble: |
  universe u v w
  namespace GeneralTopology
  structure Metric (X : Type u) where
    dist : X → X → ℝ
    symm : ∀ {x y}, dist x y = dist y x
    triangle : ∀ x y z, dist x z ≤ dist x y + dist y z
    eq_zero : ∀ {x y}, dist x y = 0 ↔ x = y

  def line : Metric ℝ where
    dist x y := |x - y|
    symm {x y} := abs_sub_comm x y
    triangle x y z := abs_sub_le x y z
    eq_zero {x y} := by rw [abs_eq_zero, sub_eq_zero]

  def Metric.ContinuousAt {X : Type u} {Y : Type v} (M : Metric X) (N : Metric Y) (f : X → Y)
      (x : X) : Prop :=
    ∀ ε > 0, ∃ δ > 0, ∀ x', M.dist x x' < δ → N.dist (f x) (f x') < ε

  def Metric.Continuous {X : Type u} {Y : Type v} (M : Metric X) (N : Metric Y) (f : X → Y) : Prop :=
    ∀ x, M.ContinuousAt N f x

  end GeneralTopology
  open GeneralTopology
description: Show that `fun t => a * t + b` is continuous on the line when `a ≠ 0`. Offer `ε / |a|`, positive by `div_pos` and `abs_pos`; `show` restates the goal as the absolute value it is, `ring` names the difference, and `abs_mul` splits it. `mul_div_cancel₀` finishes the arithmetic.
stub_names:
- line_continuous_affine
namespace: GeneralTopology
seq: 73
provenance:
  license: CC-BY-SA-4.0
  source: extract@9c4b8d6
  file: PolyaTopology/GeneralTopology/Continuity.lean
  line: 83
  closure_rounds: 1
  check_kind: theorem
