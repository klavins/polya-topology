slug: define_continuous
kind: definition
title: The Definition of a Continuous Map
statement: |-
  The ε-δ condition cannot be written where there are no distances. What can be written is its consequence: whenever a set is open downstream, the points that land in it form an open set upstream.

  So a map between topological spaces is *continuous* when the preimage of every open set is open. Note the direction. The condition is on preimages and not on images, and a continuous map may well carry an open set to a set that is not open — that is a fact about images, not a defect of the definition.

  We write `f ⁻¹' U` for the preimage `{x | f x ∈ U}`, and `x ∈ f ⁻¹' U` unfolds to `f x ∈ U`.
concepts:
- continuous_maps
stub: |-
  def TopologicalSpace.Continuous {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y)
      (f : X → Y) : Prop := sorry
reference: |-
  def TopologicalSpace.Continuous {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y)
      (f : X → Y) : Prop :=
    ∀ U, T'.IsOpen U → T.IsOpen (f ⁻¹' U)
spec: |-
  example {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y)
      (f : X → Y) : _root_.GeneralTopology.TopologicalSpace.Continuous T T' f ↔ (∀ U, T'.IsOpen U → T.IsOpen (f ⁻¹' U)) := by unfold _root_.GeneralTopology.TopologicalSpace.Continuous; first | exact Iff.rfl | grind
  example (X Y : Type) (T : TopologicalSpace X) (T' : TopologicalSpace Y) (f : X → Y) :
      T.Continuous T' f ↔ ∀ U, T'.IsOpen U → T.IsOpen (f ⁻¹' U) := Iff.rfl
preamble: |
  universe u v w
  namespace GeneralTopology
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  end GeneralTopology
  open GeneralTopology
description: 'Define `TopologicalSpace.Continuous`, taking a topology `T` on `X`, a topology `T''` on `Y` and a map `f : X → Y`: every set `T''` calls open has a preimage `T` calls open.'
stub_names:
- TopologicalSpace.Continuous
namespace: GeneralTopology
seq: 74
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/Continuity.lean
  line: 121
  closure_rounds: 1
  check_kind: definition
