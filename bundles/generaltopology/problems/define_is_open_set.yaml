slug: define_is_open_set
kind: definition
title: Open Sets
statement: |-
  A subset `U` of a metric space is *open* when every point of it has room to spare: for each `x` in `U` there is a radius `ε > 0`, however small, with the whole ball `M.ball x ε` inside `U`. No point of an open set sits on its edge.

  The radius depends on the point, and that is the whole content of the definition. A point deep inside `U` has a generous ball; one near the edge has a small one; what is asked is only that some positive radius works for each.

  `∃ ε > 0, p ε` abbreviates `∃ ε, ε > 0 ∧ p ε`.
concepts:
- open_sets
stub: 'def Metric.IsOpenSet {X : Type u} (M : Metric X) (U : Set X) : Prop := sorry'
reference: |-
  def Metric.IsOpenSet {X : Type u} (M : Metric X) (U : Set X) : Prop :=
    ∀ x ∈ U, ∃ ε > 0, M.ball x ε ⊆ U
spec: |-
  example {X : Type u} (M : Metric X) (U : Set X) : _root_.GeneralTopology.Metric.IsOpenSet M U ↔ (∀ x ∈ U, ∃ ε > 0, M.ball x ε ⊆ U) := by unfold _root_.GeneralTopology.Metric.IsOpenSet; first | exact Iff.rfl | grind
  example (X : Type) (M : Metric X) (U : Set X) :
      M.IsOpenSet U ↔ ∀ x ∈ U, ∃ ε > 0, M.ball x ε ⊆ U := Iff.rfl
preamble: |
  universe u
  namespace GeneralTopology
  structure Metric (X : Type u) where
    dist : X → X → ℝ
    symm : ∀ {x y}, dist x y = dist y x
    triangle : ∀ x y z, dist x z ≤ dist x y + dist y z
    eq_zero : ∀ {x y}, dist x y = 0 ↔ x = y

  def Metric.ball {X : Type u} (M : Metric X) (x : X) (ε : ℝ) : Set X :=
    {y | M.dist x y < ε}

  end GeneralTopology
  open GeneralTopology
description: 'Define `Metric.IsOpenSet`, which says that `U` is open in the metric space `M`: for every `x` in `U` there is a positive `ε` with `M.ball x ε ⊆ U`.'
stub_names:
- Metric.IsOpenSet
namespace: GeneralTopology
seq: 31
provenance:
  license: CC-BY-SA-4.0
  source: extract@332492a
  file: PolyaTopology/GeneralTopology/TopologicalSpaces.lean
  line: 22
  closure_rounds: 1
  check_kind: definition
