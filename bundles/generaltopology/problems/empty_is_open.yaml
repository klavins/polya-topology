slug: empty_is_open
kind: theorem
title: The Empty Set is Open
statement: 'The empty set was not asked for, and does not need to be: it is the union of no sets at all. Uniting the members of the empty collection leaves nothing, so `⋃₀ ∅ = ∅`, and the union condition applies to the empty collection as to any other — its hypothesis, that every member is open, is satisfied because it has no members.'
concepts:
- topological_space
stub: 'theorem TopologicalSpace.empty {X : Type u} (T : TopologicalSpace X) : T.IsOpen (∅ : Set X) := sorry'
reference: |-
  theorem TopologicalSpace.empty {X : Type u} (T : TopologicalSpace X) : T.IsOpen (∅ : Set X) := by
    have h : T.IsOpen (⋃₀ (∅ : Set (Set X))) := T.isOpen_sUnion (fun _ hU => hU.elim)
    rwa [Set.sUnion_empty] at h
spec: 'example : ∀ {X : Type u} (T : TopologicalSpace X), T.IsOpen (∅ : Set X) := _root_.GeneralTopology.TopologicalSpace.empty'
preamble: |
  universe u
  namespace GeneralTopology
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  end GeneralTopology
  open GeneralTopology
description: Show that `∅` is open in any topology. Apply `T.isOpen_sUnion` to the empty collection, whose hypothesis holds because a membership in `∅` is absurd — `fun _ hU => hU.elim` — and then rewrite with `Set.sUnion_empty`.
stub_names:
- TopologicalSpace.empty
namespace: GeneralTopology
seq: 37
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/TopologicalSpaces.lean
  line: 164
  closure_rounds: 1
  check_kind: theorem
