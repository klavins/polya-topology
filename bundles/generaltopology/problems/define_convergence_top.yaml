slug: define_convergence_top
kind: example
title: Convergence in a Topological Space
statement: |-
  Read the metric definition again with the balls hidden. A sequence converges to `a` when, for each way of saying *near `a`*, the terms are eventually there. In a topological space the ways of saying it are the neighbourhoods of `a`, and the definition is the same sentence with a neighbourhood `N` in place of the ball. Mathlib writes this limit `Tendsto x atTop (𝓝 a)`, and `Metric.tendsto_atTop` proves it the same as the ε–N form.

  The constant sequence converges to its value here too, and for less reason than before: every neighbourhood of `a` contains `a`, so every term of the sequence is in it from the start.
concepts:
- convergence_topological
stub: |-
  def TopologicalSpace.ConvergesTo {X : Type u} (T : TopologicalSpace X) (x : ℕ → X) (a : X) : Prop := sorry

  theorem TopologicalSpace.convergesTo_const {X : Type u} (T : TopologicalSpace X) (a : X) :
      T.ConvergesTo (fun _ => a) a := sorry
reference: |-
  def TopologicalSpace.ConvergesTo {X : Type u} (T : TopologicalSpace X) (x : ℕ → X) (a : X) : Prop :=
    ∀ N, T.IsNbhd a N → ∃ k : ℕ, ∀ n ≥ k, x n ∈ N

  theorem TopologicalSpace.convergesTo_const {X : Type u} (T : TopologicalSpace X) (a : X) :
      T.ConvergesTo (fun _ => a) a := by
    rintro N ⟨U, _, haU, hUN⟩
    exact ⟨0, fun _ _ => hUN haU⟩
spec: |-
  example {X : Type u} (T : TopologicalSpace X) (x : ℕ → X) (a : X) : _root_.GeneralTopology.TopologicalSpace.ConvergesTo T x a ↔ (∀ N, T.IsNbhd a N → ∃ k : ℕ, ∀ n ≥ k, x n ∈ N) := by unfold _root_.GeneralTopology.TopologicalSpace.ConvergesTo; first | exact Iff.rfl | grind
  example : ∀ {X : Type u} (T : TopologicalSpace X) (a : X), T.ConvergesTo (fun _ => a) a := _root_.GeneralTopology.TopologicalSpace.convergesTo_const
  example (X : Type) (T : TopologicalSpace X) (x : ℕ → X) (a : X) :
      T.ConvergesTo x a ↔ ∀ N, T.IsNbhd a N → ∃ k : ℕ, ∀ n ≥ k, x n ∈ N := Iff.rfl
preamble: |
  universe u v
  namespace GeneralTopology
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  def TopologicalSpace.IsNbhd {X : Type u} (T : TopologicalSpace X) (x : X) (N : Set X) : Prop :=
    ∃ U, T.IsOpen U ∧ x ∈ U ∧ U ⊆ N

  end GeneralTopology
  open GeneralTopology
description: 'Define `TopologicalSpace.ConvergesTo`, taking a topology `T` on `X`, a sequence `x : ℕ → X` and a point `a`: for every `N` with `T.IsNbhd a N` there is a `k : ℕ` with `x n ∈ N` for every `n ≥ k`. Then prove `TopologicalSpace.convergesTo_const`, for which `rintro` takes the neighbourhood apart into the open set inside it.'
stub_names:
- TopologicalSpace.ConvergesTo
- TopologicalSpace.convergesTo_const
namespace: GeneralTopology
seq: 184
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/Sequences.lean
  line: 142
  closure_rounds: 1
  check_kind: mixed
