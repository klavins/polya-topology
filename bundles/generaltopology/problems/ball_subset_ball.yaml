slug: ball_subset_ball
kind: theorem
title: A Ball Inside a Ball About Another Centre
statement: |-
  Here is the triangle inequality translated into the language of balls, which is the form in which it is almost always used.

  Let `x` and `y` be two points, at distance `M.dist x y` from one another. A point `z` within `ε` of `y` is then within `M.dist x y + ε` of `x`, since the journey from `x` to `z` may be made through `y`. So a ball about `y` sits inside a ball about `x`, and the price of moving the centre is exactly the distance the centre moved.

  One consequence is that the centre of a ball hardly matters. Any ball, about any point, is contained in some ball about any other point; only the radius has to grow.
concepts:
- open_ball
stub: |-
  theorem MetricSpace.ball_subset_ball {X : Type u} (M : MetricSpace X) (x y : X) (ε : ℝ) :
      M.ball y ε ⊆ M.ball x (M.dist x y + ε) := sorry
reference: |-
  theorem MetricSpace.ball_subset_ball {X : Type u} (M : MetricSpace X) (x y : X) (ε : ℝ) :
      M.ball y ε ⊆ M.ball x (M.dist x y + ε) := by
    intro z hz
    rw [M.mem_ball] at hz ⊢
    have h := M.dist_triangle x y z
    linarith
spec: 'example : ∀ {X : Type u} (M : MetricSpace X) (x y : X) (ε : ℝ), M.ball y ε ⊆ M.ball x (M.dist x y + ε) := _root_.GeneralTopology.MetricSpace.ball_subset_ball'
preamble: |
  universe u
  namespace GeneralTopology
  structure MetricSpace (X : Type u) where
    dist : X → X → ℝ
    dist_comm : ∀ {x y}, dist x y = dist y x
    dist_triangle : ∀ x y z, dist x z ≤ dist x y + dist y z
    dist_eq_zero : ∀ {x y}, dist x y = 0 ↔ x = y

  def MetricSpace.ball {X : Type u} (M : MetricSpace X) (x : X) (ε : ℝ) : Set X :=
    {y | M.dist x y < ε}

  theorem MetricSpace.mem_ball {X : Type u} (M : MetricSpace X) {x y : X} {ε : ℝ} :
      y ∈ M.ball x ε ↔ M.dist x y < ε := Iff.rfl

  end GeneralTopology
  open GeneralTopology
description: Show that `M.ball y ε ⊆ M.ball x (M.dist x y + ε)`. Introduce a point `z` and its membership, rewrite both that hypothesis and the goal with `M.mem_ball`, and give `linarith` the instance `M.dist_triangle x y z` of the triangle inequality.
stub_names:
- MetricSpace.ball_subset_ball
namespace: GeneralTopology
seq: 9
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/MetricSpaces.lean
  line: 248
  closure_rounds: 1
  check_kind: theorem
