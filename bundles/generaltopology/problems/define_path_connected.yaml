slug: define_path_connected
kind: example
title: Paths and Path-Connected Spaces
statement: |-
  The unit interval carries the topology it inherits from the line, and a *path* in a space `X` is a continuous map from the interval into `X`. Its endpoints are its values at the two ends of the interval, which as points of the subspace are the numbers with their memberships attached: `unitInterval.zero` and `unitInterval.one` are given below.

  A space is *path-connected* when any two of its points are the endpoints of a path, the first point at the start.
concepts:
- path_connected
stub: |-
  def unitInterval.zero : unitInterval := ⟨0, le_refl 0, by norm_num⟩

  def unitInterval.one : unitInterval := ⟨1, by norm_num, le_refl 1⟩

  def Topology.IsPathConnected {X : Type u} (T : Topology X) : Prop := sorry
reference: |-
  def unitInterval.zero : unitInterval := ⟨0, le_refl 0, by norm_num⟩

  def unitInterval.one : unitInterval := ⟨1, by norm_num, le_refl 1⟩

  def Topology.IsPathConnected {X : Type u} (T : Topology X) : Prop :=
    ∀ x y : X, ∃ γ : unitInterval → X,
      (line.toTopology.subspace unitInterval).Continuous T γ ∧
        γ unitInterval.zero = x ∧ γ unitInterval.one = y
spec: |-
  example : _root_.GeneralTopology.unitInterval.zero = (⟨0, le_refl 0, by norm_num⟩ : unitInterval) := rfl
  example : _root_.GeneralTopology.unitInterval.one = (⟨1, by norm_num, le_refl 1⟩ : unitInterval) := rfl
  example {X : Type u} (T : Topology X) : _root_.GeneralTopology.Topology.IsPathConnected T ↔ (∀ x y : X, ∃ γ : unitInterval → X,
      (line.toTopology.subspace unitInterval).Continuous T γ ∧
        γ _root_.GeneralTopology.unitInterval.zero = x ∧ γ _root_.GeneralTopology.unitInterval.one = y) := by unfold _root_.GeneralTopology.Topology.IsPathConnected; first | exact Iff.rfl | grind
  example (X : Type) (T : Topology X) :
      T.IsPathConnected ↔ ∀ x y : X, ∃ γ : unitInterval → X,
        (line.toTopology.subspace unitInterval).Continuous T γ ∧
          γ _root_.GeneralTopology.unitInterval.zero = x ∧ γ _root_.GeneralTopology.unitInterval.one = y := Iff.rfl
preamble: |
  universe u v w
  namespace GeneralTopology
  structure Metric (X : Type u) where
    dist : X → X → ℝ
    symm : ∀ {x y}, dist x y = dist y x
    triangle : ∀ x y z, dist x z ≤ dist x y + dist y z
    eq_zero : ∀ {x y}, dist x y = 0 ↔ x = y

  def line : Metric ℝ where
    dist x y := |x - y|
    symm {x y} := abs_sub_comm x y
    triangle x y z := abs_sub_le x y z
    eq_zero {x y} := by rw [abs_eq_zero, sub_eq_zero]

  def Metric.ball {X : Type u} (M : Metric X) (x : X) (ε : ℝ) : Set X :=
    {y | M.dist x y < ε}

  theorem Metric.mem_ball {X : Type u} (M : Metric X) {x y : X} {ε : ℝ} :
      y ∈ M.ball x ε ↔ M.dist x y < ε := Iff.rfl

  def Metric.IsOpenSet {X : Type u} (M : Metric X) (U : Set X) : Prop :=
    ∀ x ∈ U, ∃ ε > 0, M.ball x ε ⊆ U

  theorem Metric.isOpenSet_univ {X : Type u} (M : Metric X) : M.IsOpenSet (Set.univ : Set X) :=
    fun _ _ => ⟨1, one_pos, fun _ _ => trivial⟩

  theorem Metric.isOpenSet_inter {X : Type u} (M : Metric X) {U V : Set X}
      (hU : M.IsOpenSet U) (hV : M.IsOpenSet V) : M.IsOpenSet (U ∩ V) := by
    intro x hx
    obtain ⟨a, ha, hau⟩ := hU x hx.1
    obtain ⟨b, hb, hbv⟩ := hV x hx.2
    refine ⟨min a b, lt_min ha hb, ?_⟩
    intro y hy
    rw [M.mem_ball] at hy
    exact ⟨hau (lt_of_lt_of_le hy (min_le_left a b)), hbv (lt_of_lt_of_le hy (min_le_right a b))⟩

  theorem Metric.isOpenSet_sUnion {X : Type u} (M : Metric X) {F : Set (Set X)}
      (h : ∀ U ∈ F, M.IsOpenSet U) : M.IsOpenSet (⋃₀ F) := by
    intro x hx
    obtain ⟨U, hUF, hxU⟩ := hx
    obtain ⟨ε, hε, hsub⟩ := h U hUF x hxU
    exact ⟨ε, hε, fun y hy => ⟨U, hUF, hsub hy⟩⟩

  structure Topology (X : Type u) where
    IsOpen : Set X → Prop
    univ : IsOpen Set.univ
    inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  def Metric.toTopology {X : Type u} (M : Metric X) : Topology X where
    IsOpen := M.IsOpenSet
    univ := M.isOpenSet_univ
    inter := M.isOpenSet_inter
    sUnion := M.isOpenSet_sUnion

  def Topology.subspace {X : Type u} (T : Topology X) (A : Set X) : Topology A where
    IsOpen := fun V => ∃ U, T.IsOpen U ∧ V = Subtype.val ⁻¹' U
    univ := ⟨Set.univ, T.univ, rfl⟩
    inter := by
      rintro V W ⟨U, hU, rfl⟩ ⟨U', hU', rfl⟩
      exact ⟨U ∩ U', T.inter hU hU', rfl⟩
    sUnion := by
      intro F h
      refine ⟨⋃₀ {U | T.IsOpen U ∧ (Subtype.val ⁻¹' U : Set A) ∈ F}, T.sUnion (fun U hU => hU.1), ?_⟩
      apply Set.Subset.antisymm
      · rintro x ⟨V, hVF, hxV⟩
        obtain ⟨U, hU, rfl⟩ := h V hVF
        exact ⟨U, ⟨hU, hVF⟩, hxV⟩
      · rintro x ⟨U, ⟨hU, hUF⟩, hxU⟩
        exact ⟨_, hUF, hxU⟩

  def Topology.Continuous {X : Type u} {Y : Type v} (T : Topology X) (T' : Topology Y)
      (f : X → Y) : Prop :=
    ∀ U, T'.IsOpen U → T.IsOpen (f ⁻¹' U)

  def unitInterval : Set ℝ := {x | 0 ≤ x ∧ x ≤ 1}

  end GeneralTopology
  open GeneralTopology
description: 'Define `Topology.IsPathConnected`: for all `x y : X` there is a `γ : unitInterval → X` which is continuous from `line.toTopology.subspace unitInterval` to `T`, with `γ unitInterval.zero = x` and `γ unitInterval.one = y`, the three conditions in that order.'
stub_names:
- unitInterval.zero
- unitInterval.one
- Topology.IsPathConnected
namespace: GeneralTopology
seq: 177
provenance:
  license: CC-BY-SA-4.0
  source: extract@9c4b8d6
  file: PolyaTopology/GeneralTopology/Connectedness.lean
  line: 585
  closure_rounds: 1
  check_kind: mixed
