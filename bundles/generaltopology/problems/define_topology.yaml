slug: define_topology
kind: definition
title: The Definition of a Topological Space
statement: |-
  A *topology* on a set `X` names some of its subsets open, subject to three conditions — the three facts that were just proved about a metric space, and nothing more.

  The whole set is open. The intersection of two open sets is open. The union of any collection of open sets is open, the collection being a `Set (Set X)` and the union `⋃₀ F`.

  Two and any number are not the same condition, and the asymmetry is the point: intersecting needs a smallest radius, and only finitely many radii are sure to have one.
concepts:
- topological_space
stub: |-
  structure TopologicalSpace (X : Type u) where
    IsOpen : sorry
    isOpen_univ : sorry
    isOpen_inter : sorry
    isOpen_sUnion : sorry
reference: |-
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)
spec: |-
  example (X : Type) (T : _root_.GeneralTopology.TopologicalSpace X) (U V : Set X) (F : Set (Set X)) :
      T.IsOpen Set.univ
        ∧ (T.IsOpen U → T.IsOpen V → T.IsOpen (U ∩ V))
        ∧ ((∀ W ∈ F, T.IsOpen W) → T.IsOpen (⋃₀ F)) := by
    refine ⟨T.isOpen_univ, fun _ _ => ?_, fun _ => ?_⟩
    · apply T.isOpen_inter <;> assumption
    · apply T.isOpen_sUnion
      assumption
preamble: |
  universe u
description: 'Define the structure `TopologicalSpace X`, with a field `IsOpen : Set X → Prop` and then `univ`, `inter` and `sUnion` carrying the three conditions. Bind the sets implicitly in `inter` and `sUnion`, since a proof using them names the hypotheses rather than the sets.'
stub_names:
- TopologicalSpace
namespace: GeneralTopology
seq: 36
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/TopologicalSpaces.lean
  line: 131
  closure_rounds: 1
  check_kind: definition
