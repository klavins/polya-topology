slug: define_continuous_at_top
kind: definition
title: Continuity at a Point, Without Distances
statement: |-
  The ε-δ definition was local: it spoke of one point at a time. The topological definition is global, and a local form of it is wanted too. Neighbourhoods supply one, since a neighbourhood is what a ball became.

  A map is *continuous at* `x` when every neighbourhood of `f x` has a preimage that is a neighbourhood of `x`. Read with balls in a metric space, that is the ε-δ condition at `x` with the quantifiers hidden inside the word "neighbourhood".
concepts:
- continuity_closed
stub: |-
  def TopologicalSpace.ContinuousAt {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y) (f : X → Y)
      (x : X) : Prop := sorry
reference: |-
  def TopologicalSpace.ContinuousAt {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y) (f : X → Y)
      (x : X) : Prop :=
    ∀ N, T'.IsNbhd (f x) N → T.IsNbhd x (f ⁻¹' N)
spec: |-
  example {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y) (f : X → Y)
      (x : X) : _root_.GeneralTopology.TopologicalSpace.ContinuousAt T T' f x ↔ (∀ N, T'.IsNbhd (f x) N → T.IsNbhd x (f ⁻¹' N)) := by unfold _root_.GeneralTopology.TopologicalSpace.ContinuousAt; first | exact Iff.rfl | grind
  example (X Y : Type) (T : TopologicalSpace X) (T' : TopologicalSpace Y) (f : X → Y) (x : X) :
      T.ContinuousAt T' f x ↔ ∀ N, T'.IsNbhd (f x) N → T.IsNbhd x (f ⁻¹' N) := Iff.rfl
preamble: |
  universe u v w
  namespace GeneralTopology
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  def TopologicalSpace.IsNbhd {X : Type u} (T : TopologicalSpace X) (x : X) (N : Set X) : Prop :=
    ∃ U, T.IsOpen U ∧ x ∈ U ∧ U ⊆ N

  end GeneralTopology
  open GeneralTopology
description: 'Define `TopologicalSpace.ContinuousAt`, taking topologies `T` on `X` and `T''` on `Y`, a map `f`, and a point `x` of `X`: every neighbourhood `N` of `f x` has `f ⁻¹'' N` a neighbourhood of `x`.'
stub_names:
- TopologicalSpace.ContinuousAt
namespace: GeneralTopology
seq: 81
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/Continuity.lean
  line: 308
  closure_rounds: 1
  check_kind: definition
