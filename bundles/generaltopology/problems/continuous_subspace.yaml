slug: continuous_subspace
kind: example
title: A Subspace Sits in its Space Continuously
statement: |-
  The subspace topology was defined by declaring a subset of `A` open when an open set of `X` cuts it out. That is exactly what it takes to make the inclusion continuous, and the inclusion's continuity is the reason the definition is the one it is.

  `Subtype.val` is the inclusion, sending a point of `A` — a point of `X` with a proof it lies in `A` — back to the point it was. A preimage under it is the trace on `A` of a set of `X`, so the preimage of an open set is open by definition, with nothing to check.

  Composing then restricts any continuous map to a subspace of its source.
concepts:
- continuity_examples
stub: |-
  theorem TopologicalSpace.continuous_subspace_val {X : Type u} (T : TopologicalSpace X) (A : Set X) :
      (T.subspace A).Continuous T Subtype.val := sorry

  theorem TopologicalSpace.continuous_restrict {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      {T' : TopologicalSpace Y}
      {f : X → Y} (hf : T.Continuous T' f) (A : Set X) :
      (T.subspace A).Continuous T' (f ∘ Subtype.val) := sorry
reference: |-
  theorem TopologicalSpace.continuous_subspace_val {X : Type u} (T : TopologicalSpace X) (A : Set X) :
      (T.subspace A).Continuous T Subtype.val :=
    fun U hU => ⟨U, hU, rfl⟩

  theorem TopologicalSpace.continuous_restrict {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      {T' : TopologicalSpace Y}
      {f : X → Y} (hf : T.Continuous T' f) (A : Set X) :
      (T.subspace A).Continuous T' (f ∘ Subtype.val) :=
    (T.subspace A).continuous_comp (T.continuous_subspace_val A) hf
spec: |-
  example : ∀ {X : Type u} (T : TopologicalSpace X) (A : Set X), (T.subspace A).Continuous T Subtype.val := _root_.GeneralTopology.TopologicalSpace.continuous_subspace_val
  example : ∀ {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      {T' : TopologicalSpace Y}
      {f : X → Y} (hf : T.Continuous T' f) (A : Set X), (T.subspace A).Continuous T' (f ∘ Subtype.val) := _root_.GeneralTopology.TopologicalSpace.continuous_restrict
preamble: |
  universe u v w
  namespace GeneralTopology
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  def TopologicalSpace.subspace {X : Type u} (T : TopologicalSpace X) (A : Set X)
      : TopologicalSpace A where
    IsOpen := fun V => ∃ U, T.IsOpen U ∧ V = Subtype.val ⁻¹' U
    isOpen_univ := ⟨Set.univ, T.isOpen_univ, rfl⟩
    isOpen_inter := by
      rintro V W ⟨U, hU, rfl⟩ ⟨U', hU', rfl⟩
      exact ⟨U ∩ U', T.isOpen_inter hU hU', rfl⟩
    isOpen_sUnion := by
      intro F h
      refine ⟨⋃₀ {U | T.IsOpen U ∧ (Subtype.val ⁻¹' U : Set A) ∈ F},
        T.isOpen_sUnion (fun U hU => hU.1), ?_⟩
      apply Set.Subset.antisymm
      · rintro x ⟨V, hVF, hxV⟩
        obtain ⟨U, hU, rfl⟩ := h V hVF
        exact ⟨U, ⟨hU, hVF⟩, hxV⟩
      · rintro x ⟨U, ⟨hU, hUF⟩, hxU⟩
        exact ⟨_, hUF, hxU⟩

  def TopologicalSpace.Continuous {X : Type u} {Y : Type v} (T : TopologicalSpace X)
      (T' : TopologicalSpace Y)
      (f : X → Y) : Prop :=
    ∀ U, T'.IsOpen U → T.IsOpen (f ⁻¹' U)

  theorem TopologicalSpace.continuous_comp {X : Type u} {Y : Type v} {Z : Type w}
      (T : TopologicalSpace X)
      {T' : TopologicalSpace Y} {T'' : TopologicalSpace Z} {f : X → Y} {g : Y → Z}
      (hf : T.Continuous T' f) (hg : T'.Continuous T'' g) : T.Continuous T'' (g ∘ f) := by
    intro U hU
    rw [Set.preimage_comp]
    exact hf _ (hg U hU)

  end GeneralTopology
  open GeneralTopology
description: Show that the inclusion of a subspace is continuous, and that a continuous map composed with it is continuous. The first is an anonymous constructor whose third component is `rfl`; the second is the composition law, applied to the two maps in the right order.
stub_names:
- TopologicalSpace.continuous_subspace_val
- TopologicalSpace.continuous_restrict
namespace: GeneralTopology
seq: 86
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/Continuity.lean
  line: 445
  closure_rounds: 1
  check_kind: theorem
