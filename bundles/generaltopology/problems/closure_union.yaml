slug: closure_union
kind: theorem
title: The Closure of a Union of Two Sets
statement: |-
  Closing a union of two sets is the same as closing each and uniting. This is the first statement about closures that is not a restatement of the definition, and it is proved entirely from the three properties above.

  One containment is monotonicity twice, since each set is inside the union. For the other, `T.closure S ∪ T.closure S'` is a closed set — a union of two closed sets is closed — and it contains `S ∪ S'`, so it contains the closure of the union.

  Two is essential. For infinitely many sets the union of the closures need not be closed, and the statement fails.
concepts:
- closure
stub: |-
  theorem TopologicalSpace.closure_union {X : Type u} (T : TopologicalSpace X) (S S' : Set X) :
      T.closure (S ∪ S') = T.closure S ∪ T.closure S' := sorry
reference: |-
  theorem TopologicalSpace.closure_union {X : Type u} (T : TopologicalSpace X) (S S' : Set X) :
      T.closure (S ∪ S') = T.closure S ∪ T.closure S' := by
    apply Set.Subset.antisymm
    · apply T.closure_min (T.isClosed_union (T.isClosed_closure S) (T.isClosed_closure S'))
      exact Set.union_subset_union (T.subset_closure S) (T.subset_closure S')
    · exact Set.union_subset (T.closure_mono Set.subset_union_left)
        (T.closure_mono Set.subset_union_right)
spec: 'example : ∀ {X : Type u} (T : TopologicalSpace X) (S S'' : Set X), T.closure (S ∪ S'') = T.closure S ∪ T.closure S'' := _root_.GeneralTopology.TopologicalSpace.closure_union'
preamble: |
  universe u
  namespace GeneralTopology
  structure TopologicalSpace (X : Type u) where
    IsOpen : Set X → Prop
    isOpen_univ : IsOpen Set.univ
    isOpen_inter : ∀ {U V}, IsOpen U → IsOpen V → IsOpen (U ∩ V)
    isOpen_sUnion : ∀ {F : Set (Set X)}, (∀ U ∈ F, IsOpen U) → IsOpen (⋃₀ F)

  def TopologicalSpace.IsClosed {X : Type u} (T : TopologicalSpace X) (C : Set X) : Prop
      := T.IsOpen Cᶜ

  theorem TopologicalSpace.isClosed_union {X : Type u} (T : TopologicalSpace X) {C D : Set X}
      (hC : T.IsClosed C) (hD : T.IsClosed D) : T.IsClosed (C ∪ D) := by
    show T.IsOpen _
    rw [Set.compl_union]
    exact T.isOpen_inter hC hD

  theorem TopologicalSpace.isClosed_sInter {X : Type u} (T : TopologicalSpace X) {F : Set (Set X)}
      (h : ∀ C ∈ F, T.IsClosed C) : T.IsClosed (⋂₀ F) := by
    show T.IsOpen _
    rw [Set.compl_sInter]
    apply T.isOpen_sUnion
    rintro U ⟨C, hCF, rfl⟩
    exact h C hCF

  def TopologicalSpace.closure {X : Type u} (T : TopologicalSpace X) (S : Set X) : Set X :=
    ⋂₀ {C | T.IsClosed C ∧ S ⊆ C}

  theorem TopologicalSpace.subset_closure {X : Type u} (T : TopologicalSpace X) (S : Set X)
      : S ⊆ T.closure S := by
    intro x hx C hC
    exact hC.2 hx

  theorem TopologicalSpace.isClosed_closure {X : Type u} (T : TopologicalSpace X) (S : Set X) :
      T.IsClosed (T.closure S) :=
    T.isClosed_sInter (fun _ hC => hC.1)

  theorem TopologicalSpace.closure_min {X : Type u} (T : TopologicalSpace X) {S C : Set X}
      (hC : T.IsClosed C) (hSC : S ⊆ C) : T.closure S ⊆ C := by
    intro x hx
    exact hx C ⟨hC, hSC⟩

  theorem TopologicalSpace.closure_mono {X : Type u} (T : TopologicalSpace X) {S S' : Set X}
      (h : S ⊆ S') :
      T.closure S ⊆ T.closure S' :=
    T.closure_min (T.isClosed_closure S') (h.trans (T.subset_closure S'))

  end GeneralTopology
  open GeneralTopology
description: Prove that `T.closure (S ∪ S') = T.closure S ∪ T.closure S'`. Split with `Set.Subset.antisymm`; `Set.union_subset_union` and `Set.union_subset` combine two containments into one, and `Set.subset_union_left` and `Set.subset_union_right` put a set inside a union.
stub_names:
- TopologicalSpace.closure_union
namespace: GeneralTopology
seq: 59
provenance:
  license: CC-BY-SA-4.0
  source: extract@62a7d2b
  file: PolyaTopology/GeneralTopology/Closure.lean
  line: 167
  closure_rounds: 1
  check_kind: theorem
